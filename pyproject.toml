[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["app"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py313"
line-length = 100
src = ["app/app", "app/tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "ASYNC"]

